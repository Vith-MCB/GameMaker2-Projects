//Creating the camera 

//Variables
target = objPlayer; //target that will be followed
width = camera_get_view_width(view_camera[0]); //get viewport width
height = camera_get_view_height(view_camera[0]); //get viewport height
